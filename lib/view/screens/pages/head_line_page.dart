import 'package:flutter/material.dart';

class HeadLinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        // TODO
        body: Center(
          child: Center(child: Text("トップニュース")),
        ),
      ),
    );
  }
}
